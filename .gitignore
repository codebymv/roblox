# Built Roblox places / models (regenerate with: rojo build)
*.rbxl
*.rbxlx
*.rbxm
*.rbxmx

# Rojo / toolchain
sourcemap.json
*.project.json.bak

# Derived from the live Roblox API dump; regenerate rather than commit.
# selene generate-roblox-std
roblox.yml
# curl the copy from the luau-lsp repo (see .github/workflows/ci.yml)
globalTypes.d.luau

# OS
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Local tooling / deps
node_modules/
.aftman/
.rokit/
Packages/
ServerPackages/
*.log
*.tmp

# Secrets / local env (never commit)
.env
.env.*
*.pem
*.key
credentials.json
